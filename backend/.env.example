# Server Configuration
PORT=5001

# MongoDB Connection
# For local MongoDB:
# MONGO_URI=mongodb://localhost:27017/handicraft
# For MongoDB Atlas (cloud):
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/handicraft?retryWrites=true&w=majority

# JWT Secret (generate a strong random string for production)
JWT_SECRET=your-super-secret-jwt-key-here

# Cloudinary Configuration (https://cloudinary.com)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Admin Seed Credentials (for running: npx ts-node src/scripts/seedAdmin.ts)
# Password must be: 8+ chars, uppercase, lowercase, number, special char
ADMIN_EMAIL=admin@yourcompany.com
ADMIN_PASSWORD=YourSecurePassword123!
